export type User = {
    id: number,
    first_name: string,
    last_name: string,
    email: string,
    password: string,
    linkedin_url: string,
    github_url: string,
    instagram_url: string,
    role: string;
}
